export const registerUser = (_data: any) => {
  // Dummy implementation for now (will replace in next steps)
  return "dummy-token";
};

export const loginUser = (_data: any) => {
  // Dummy implementation for now (will replace in next steps)
  return "dummy-token";
};
